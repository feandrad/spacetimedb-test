// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct EnemyAttackPlayerArgs {
    pub enemy_id: u32,
    pub player_id: u32,
    pub damage: f32,
}

impl From<EnemyAttackPlayerArgs> for super::Reducer {
    fn from(args: EnemyAttackPlayerArgs) -> Self {
        Self::EnemyAttackPlayer {
            enemy_id: args.enemy_id,
            player_id: args.player_id,
            damage: args.damage,
        }
    }
}

impl __sdk::InModule for EnemyAttackPlayerArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `enemy_attack_player`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait enemy_attack_player {
    /// Request that the remote module invoke the reducer `enemy_attack_player` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`enemy_attack_player:enemy_attack_player_then`] to run a callback after the reducer completes.
    fn enemy_attack_player(&self, enemy_id: u32, player_id: u32, damage: f32) -> __sdk::Result<()> {
        self.enemy_attack_player_then(enemy_id, player_id, damage, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `enemy_attack_player` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn enemy_attack_player_then(
        &self,
        enemy_id: u32,
        player_id: u32,
        damage: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl enemy_attack_player for super::RemoteReducers {
    fn enemy_attack_player_then(
        &self,
        enemy_id: u32,
        player_id: u32,
        damage: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            EnemyAttackPlayerArgs {
                enemy_id,
                player_id,
                damage,
            },
            callback,
        )
    }
}
