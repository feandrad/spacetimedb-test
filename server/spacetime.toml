[project]
name = "guildmaster"
description = "Guildmaster MVP server using SpacetimeDB"

[database]
# Database configuration
# The server will be deployed to SpacetimeDB cloud or local instance

[build]
# Build configuration for the Rust module
target = "wasm32-unknown-unknown"